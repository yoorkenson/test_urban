import React, { FC } from 'react';
import LoginForm from '../components/LoginForm';

const Login: FC = () => {
    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export default Login;